spring:
  application:
    name: "user-service"

server:
    port: 8085

eureka:
    client:
        service-url:
            defaultZone: "http://localhost:8888/eureka/"
        register-with-eureka: true
        fetch-registry: true

